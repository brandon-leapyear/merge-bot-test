version: 2

jobs:
  foo:
    docker:
      - image: *docker-image
    steps:
      - run: echo foo
  bar:
    docker:
      - image: centos:7
    steps:

workflows:
  version: 2
  run_foo:
    jobs:
      - foo:
          filters:
            branches:
              only:
                - /trying-.*/
                - /staging-.*/
      - bar:
          requires:
            - foo
